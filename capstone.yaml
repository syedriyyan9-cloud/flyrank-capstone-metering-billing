name: Usage Metering & Billing Engine
version: 1.0.0

run: docker-compose up --build
seed: docker exec -it metering_app python scripts/seed.py
test: python -m pytest tests/ -v

base_url: http://localhost:8000